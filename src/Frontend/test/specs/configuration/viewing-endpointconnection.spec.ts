import { it, describe } from "../../drivers/vitest/driver";

describe("FEATURE: Endpoint connection", () => {
  describe("Rule: Examples match current configuration", () => {
    it.todo("Not implemented");

    /* SCENARIO
        Endpoint Configuration only
      */

    /* SCENARIO
        Json file
      */
  });
  describe("Rule: Can copy with a single click", () => {
    it.todo("Not implemented");

    /* SCENARIO
        Endpoint Configuration only
      */

    /* SCENARIO
        Json file
      */
  });
});
