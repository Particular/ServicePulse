(function (window, angular) {
    'use strict';

    function service() {
        return {};
    }

    angular.module('sc')
        .service('failedMessagesService', service);

})(window, window.angular);