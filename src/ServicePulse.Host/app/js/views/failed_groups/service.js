; (function (window, angular, undefined) {
    'use strict';

    function service() {
        return {};
    }

    angular.module('sc')
        .service('failedMessageGroupsService', service);

})(window, window.angular);