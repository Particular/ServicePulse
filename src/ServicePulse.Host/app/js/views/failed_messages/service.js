; (function (window, angular, undefined) {
    'use strict';

    function service() {
        return {};
    }

    angular.module('sc')
        .service('failedMessagesService', service);

})(window, window.angular);