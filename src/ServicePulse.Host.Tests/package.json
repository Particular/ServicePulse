{
  "name": "ServicePulse.Host.Tests",
  "version": "1.0.0",
  "dependencies": {},
  "devDependencies": {
    "karma-chrome-launcher": "^2.2.0",
    "karma-firefox-launcher": "^1.1.0",
    "karma-jasmine": "^2.0.1",
    "karma": "^3.1.4",
    "karma-cli": "^2.0.0",
    "karma-teamcity-reporter": "^1.1.0"
  },
  "scripts": {
    "test": "karma start --browsers ChromeHeadless",
    "test-firefox": "karma start --browsers FirefoxHeadless",
    "test-chrome-teamcity": "karma start --browsers ChromeHeadless --reporters teamcity --single-run",
    "test-firefox-teamcity": "karma start --browsers FirefoxHeadless --reporters teamcity --single-run"
  }
}
